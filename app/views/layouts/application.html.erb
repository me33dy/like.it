<!DOCTYPE html>
<html>
<head>
	<link href='http://fonts.googleapis.com/css?family=Oswald:300' rel='stylesheet' type='text/css'>
  <title>Like It ? | <%= yield(:title) %></title>
  <%= stylesheet_link_tag    "application" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<% if current_user %>
		<span id ="widgettext">Welcome <strong><%= current_user.name %></strong>!</span>
		<%= link_to "Sign out", user_signout_path, id: "sign_out", class: "button tiny radius" %>
		<% else %>
		<% end %>


	
	<div id="user-widget">
		<% if current_user %>
		<div id ="nav">Welcome <strong><%= current_user.name %></strong>!
		<%= link_to "Sign out", user_signout_path, id: "sign_out" %>
		</div>
		<% else %>
		<% end %>
	</div>

		<%= yield %>


	<script type="text/javascript">
	var s = skrollr.init();
	</script>

</html>
